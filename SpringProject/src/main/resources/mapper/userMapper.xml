<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >

<mapper namespace="userMapper">

    <select id="getUserByUserId" resultType="net.study.com.user.domain.User">
        SELECT * FROM TRIP_USER WHERE USER_ID = #{userId}
    </select>


    <insert id="insertUser">
        INSERT INTO TRIP_USER
        (
        USER_NUM, USER_ID, LOGIN_ID, PASSWORD, USER_NICKNAME, USER_NAME, USER_EMAIL , USER_BIRTH, LOGIN_TYPE, REG_DATE)
        VALUES
        (
        SEQ_USERNUM.NEXTVAL,
        #{user_id},
        #{login_id},
        #{password, jdbcType=VARCHAR},
        #{user_nickname, jdbcType=VARCHAR},
        #{user_name, jdbcType=VARCHAR},
        #{user_email, jdbcType=VARCHAR},
        #{user_birth, jdbcType=VARCHAR},
        #{logintype},
        #{reg_date}
        )

    </insert>

</mapper>
